import 'package:flutter/material.dart';

class CameraToPdfScreen extends StatelessWidget {
  const CameraToPdfScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Camera to PDF')),
      body: Center(
        child: Text('Camera to PDF UI goes here'),
      ),
    );
  }
}
